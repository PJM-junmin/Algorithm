#include <iostream>
#include <algorithm>

using namespace std;



int main()
{
	// connect cin, cout, just (use cin or cout) or (use scanf or printf)
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int N, M;
	cin >> N;
	cin >> M;
	
	// vector

}